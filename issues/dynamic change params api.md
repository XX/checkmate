Проанализируй и предложи решение, как можно сделать так, чтобы некоторые параметры игры, записываемые как компоненты в ECS, можно было бы менять из сторонней программы? Например, из программы редактора. И изменения чтобы сразу применлись и были видны в игре. Предложи варианты решения без существенной потери производительности.

---

● Проанализировал код. Ниже — анализ текущего состояния и варианты решения.

  Что мешает прямо сейчас

  Два независимых препятствия, и транспорт (сокет/BRP/shm) решает только одно из них:

  1. Параметры «протекают» в компоненты один раз при спавне. Config читается в main() и значения копируются в чужие компоненты: src/main.rs:151 (DirectionalLight/SunDisk/Transform из config.environment.sun),
  src/state/ingame/engine.rs:18 (PointLight + FlickeringLight из jet_fires), AppCameraParams в main.rs:50. После спавна связи «конфиг → сущность» не существует. Часть параметров вообще захардкожена в
  Aircraft::new() / Thrust::new() (src/state/ingame/aircraft.rs:23,50).

  2. Ни один компонент не Reflect и ничего не зарегистрировано (register_type в проекте отсутствует). Из-за этого не работает не только удалённое редактирование, но и уже подключённый WorldInspectorPlugin — он
  показывает только штатные bevy-типы, ваши Aircraft, Thrust, FlickeringLight в нём невидимы.

  Поэтому решение — три слоя, и первый с третьим нужны при любом выборе транспорта:

  [редактор] --транспорт--> [слой параметров: Reflect-компоненты/ресурсы] --apply-системы--> [движковые компоненты]

  Слой параметров + применение (обязательная часть)

  Сделать параметры компонентами на сущности (а не полями глобального Config), тогда редактор адресует их по entity, а применение получается через штатное change detection — то есть бесплатно, когда никто
  ничего не крутит:

  #[derive(Component, Reflect, Debug, Clone)]
  #[reflect(Component)]
  pub struct SunParams { pub illuminance: f32, pub angular_size: f32, pub intensity: f32, pub position: Vec3, pub target: Vec3 }

  fn apply_sun(
      mut q: Query<(&SunParams, &mut DirectionalLight, &mut SunDisk, &mut Transform), Changed<SunParams>>,
  ) {
      for (p, mut light, mut disk, mut tr) in &mut q {
          light.illuminance = p.illuminance;
          disk.angular_size = p.angular_size;
          disk.intensity = p.intensity;
          *tr = Transform::from_translation(p.position).looking_at(p.target, Vec3::Y);
      }
  }

  Стоимость холостого кадра: Changed<T> — это сравнение тиков по уже существующему архетипу из одной сущности, то есть десятки наносекунд. Для ресурсов — .run_if(resource_changed::<Config>). Аналогично
  apply_jet_fire (PointLight/FlickeringLight), apply_camera (Bloom, Exposure, Tonemapping, AtmosphereSettings), apply_aircraft. Все apply-системы — в один SystemSet в PreUpdate, после системы приёма изменений.
                                                                                           
  Отдельно стоит разделить параметры на горячие (мутируются на месте: интенсивность, цвет, скорости, позиция солнца) и структурные (путь к модели, число сопел, shadow_map_size) — вторые требуют пересоздания
  сущности/ресурса, их применять через Commands и только по факту изменения, не пытаясь делать это плавно.

  Плюс register_type::<..>() для всех параметрических типов — это нулевая рантайм-стоимость и заодно чинит инспектор.
                                                                                           
  Варианты транспорта                                                                      
                                                                                           
  A. BRP — bevy_remote (рекомендую как основной)                                           
                                                                                           
  В bevy 0.19 это штатная фича (bevy_remote = ["bevy_internal/bevy_remote"], примеры лежат в examples/remote/). Подключается двумя строками:
                                                                                           
  app.add_plugins((RemotePlugin::default(), RemoteHttpPlugin::default())); // 127.0.0.1:15702
                                                                                           
  Редактор — любой JSON-RPC клиент. Методы (константы в bevy_remote::builtin_methods, строковые значения лучше брать из них, а не хардкодить): BRP_QUERY_METHOD (найти сущность по маркеру Sun),
  BRP_MUTATE_COMPONENTS_METHOD (изменить одно поле по reflect-пути, без пересылки всего компонента), BRP_GET_RESOURCE_METHOD / BRP_MUTATE_RESOURCE_METHOD, BRP_SPAWN_ENTITY_METHOD, BRP_LIST_COMPONENTS_METHOD +
  schema — редактор сам строит UI по схеме типов, без ручной синхронизации.                
                                                                                           
  Производительность: HTTP-сервер живёт в отдельной задаче, в мире выполняется только системa разбора очереди запросов; при пустой очереди — один try_recv. Один запрос — это JSON + reflect-мутация, порядка
  десятков микросекунд; при перетаскивании ползунка это 30–120 сообщений/с, то есть доли процента кадра. Слабое место — не throughput, а то, что при потоковом драге редактор должен слать дельты, а не полное
  состояние сцены.                                                                         
                                                                                           
  Обязательно спрятать за фичей/CLI-флагом (--editor), чтобы релизная сборка не открывала порт.
                                                                                           
  B. Горячая перезагрузка конфига (дёшево, дополняет A)                                    
                                                                                           
  Редактор просто пишет Config.toml, игра следит за файлом (notify в отдельном потоке → канал → ResMut<Config>, либо Config как ассет с file_watcher). Получаете бесплатно: сохранение сессии редактирования,
  работу с любым внешним редактором (хоть vim), уже готовые Serialize/Deserialize в src/config.rs. Задержка 100–300 мс, гранулярность — файл целиком (перечитали → сравнили → тронули только изменившиеся поля,
  дальше работает change detection). Как единственный механизм неудобен для ползунков, но как канал «сохранить/загрузить» нужен в любом случае — и в паре с A даёт полный цикл: крутим по BRP, потом одной
  командой сериализуем текущие параметры обратно в TOML.
                                                                                           
  C. Свой IPC: unix-сокет/TCP + postcard/bincode
                                                                                           
  Минимальный оверхед среди сетевых вариантов (бинарь вместо JSON, без reflect), двунаправленный — можно гнать телеметрию из игры в редактор (позиция самолёта, FPS, выбранная сущность) с частотой кадра. Цена —
  собственный протокол и ручная синхронизация схемы; всё, что BRP даёт даром (discovery, schema, query), придётся писать. Разумен как второй этап, если упрётесь в BRP на потоковых сценариях (драг гизмо,
  запись/воспроизведение сессии).                                                          
                                                                                           
  D. Разделяемая память (пункт из вашего бэклога)
                                                                                           
  memmap2 на файл в /dev/shm + #[repr(C)] POD-структура «горячих» тюнингов и seqlock:      
                                                                                           
  #[repr(C)]                                                                               
  struct TunableBlock {                                                                    
      version: AtomicU32,   // нечётная = идёт запись                                      
      sun_illuminance: f32,                                                                
      aircraft_max_speed: f32,                                                             
      // ... только фиксированный набор скаляров                                           
  }

  Читатель: load(Acquire) → если версия не изменилась, выходим (единицы наносекунд на кадр); иначе копируем блок и перепроверяем версию. Задержка — меньше кадра, аллокаций и сериализации нет. Это объективно
  самый быстрый вариант.                                                                   
                                                                                           
  Но: схема фиксирована и обе программы надо пересобирать согласованно; только плоские данные (никаких String, Vec, спавна сущностей, запросов); нет discovery; нужен unsafe и своя реализация под Windows.
  Поэтому я бы не делал её основным API — только «быстрой полосой» для небольшого набора непрерывно крутящихся чисел, поверх A/B. Практически: на частотах редактора (≤200 Гц изменений) разница между shm и BRP
  теряется в шуме кадра — выигрыш есть, но он не там, где узкое место.                     
                                                                                           
  E. Для сравнения: встроенный редактор                                                    
                                                                                           
  bevy-inspector-egui уже в зависимостях и после register_type даст редактирование всех параметров вообще без IPC. Если внешняя программа нужна не принципиально (а, скажем, ради удобного UI) — это нулевая
  стоимость и ноль кода транспорта. Внешний редактор оправдан, когда нужна работа с несколькими запущенными инстансами, свой UI/пайплайн ассетов или редактирование при игре в полный экран.
                                                                                           
  Сравнение                                                                                
                                                                                           
  ┌───────────────────┬────────────┬───────────────────────────┬───────────────────────────┬──────────────────┐
  │                   │  Задержка  │ Стоимость кадра вхолостую │      Схема/discovery      │   Объём работы   │
  ├───────────────────┼────────────┼───────────────────────────┼───────────────────────────┼──────────────────┤
  │ A. BRP            │ 1 кадр     │ ~0 (один try_recv)        │ автоматически, по reflect │ низкий           │
  ├───────────────────┼────────────┼───────────────────────────┼───────────────────────────┼──────────────────┤
  │ B. Файл + watcher │ 100–300 мс │ ~0                        │ ваш TOML                  │ очень низкий     │
  ├───────────────────┼────────────┼───────────────────────────┼───────────────────────────┼──────────────────┤
  │ C. Свой IPC       │ 1 кадр     │ ~0                        │ вручную                   │ средний/высокий  │
  ├───────────────────┼────────────┼───────────────────────────┼───────────────────────────┼──────────────────┤
  │ D. Shared memory  │ < 1 кадра  │ ~10 нс                    │ жёстко зашита             │ средний + unsafe │
  ├───────────────────┼────────────┼───────────────────────────┼───────────────────────────┼──────────────────┤
  │ E. egui внутри    │ мгновенно  │ ~0                        │ по reflect                │ минимальный      │
  └───────────────────┴────────────┴───────────────────────────┴───────────────────────────┴──────────────────┘

  Что предлагаю сделать                                                                    
                                                                                           
  Гибрид A + B, с общим слоем параметров; D — только если замеры покажут, что нужен:       
                                                                                           
  1. Вынести параметры из Config в Reflect-компоненты (SunParams, JetFireParams, AircraftParams, ThrustParams, CameraParams), Config оставить как источник начальных значений и формат сохранения; register_type
     для всех.                                                                             
  2. Написать apply-системы на Changed<T> в PreUpdate — это и есть «изменения сразу видны в игре».
  3. Включить bevy_remote под фичей editor, редактор ходит через bevy/query + mutate_components.
  4. Добавить watcher Config.toml и команду обратной сериализации параметров в TOML.       
  5. Только после этого, при реальной потребности, добавить shm-канал для нескольких скаляров — он ляжет поверх того же слоя параметров, apply-системы менять не придётся.
                                                                                           
  Ключевой момент: пункты 1–2 дают 80% результата и не зависят от выбора транспорта, так что начинать стоит с них — их же требует и любой из остальных вариантов.
                                                                                           
  Записать этот разбор в issues/dynamic change params api.md (там сейчас только формулировка задачи) или сразу начать с шагов 1–2?

---

Выполни шаги 1-2

---

● Ошибка на старте: конфликт доступа между запросами в apply_camera_params. Исправляю через Mut::is_added() вместо отдельного запроса.
● Шаги 1–2 выполнены, проект собирается и запускается без паник (проверено 30-секундным прогоном cargo run в состоянии InGame: ассеты грузятся, ошибок кроме предсуществующего шума Vulkan-валидации Mesa нет).

  Что появилось

  src/params.rs — ParamsPlugin: регистрация типов в реестре reflect и набор систем ApplyParamsSet в PreUpdate.

  Компоненты-параметры (источник истины во время игры, Reflect + #[reflect(Component)]):

  ┌──────────────────────────────┬──────────────────────────────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────┐
  │          Параметры           │                      Где                         │                                Применяются                                          │
  ├──────────────────────────────┼──────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────┤
  │ SunParams                    │ src/environment.rs (сюда переехал спавн солнца   │ apply_sun → DirectionalLight, SunDisk, Transform                                    │
  │                              │ из main.rs)                                      │                                                                                     │
  ├──────────────────────────────┼──────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────┤
  │ JetFireParams (+ вложенный   │ src/state/ingame/engine.rs                       │ apply_jet_fire → PointLight, FlickeringLight, Transform                             │
  │ FlickeringParams)            │                                                  │                                                                                     │
  ├──────────────────────────────┼──────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────┤
  │ TerrainParams                │ src/state/ingame/terrain.rs                      │ apply_terrain → Transform                                                           │
  ├──────────────────────────────┼──────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────┤
  │ AircraftParams, ThrustParams │ src/state/ingame/aircraft.rs                     │ системы читают их каждый кадр — применение мгновенное, отдельной системы не нужно   │
  ├──────────────────────────────┼──────────────────────────────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────┤
  │ CameraParams (ресурс)        │ src/camera.rs                                    │ apply_camera_params → Exposure, Tonemapping, Bloom, AtmosphereSettings, сглаживание │
  │                              │                                                  │ PanOrbitCamera                                                                      │
  └──────────────────────────────┴──────────────────────────────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────┘
  
  Каждый тип параметров умеет строить движковые компоненты (params.point_light(), params.transform() и т.п.), и spawn-код использует те же методы — дублирования значений между спавном и применением нет, вспышки со значениями по умолчанию на первом кадре тоже.
  
  CameraParams сделан ресурсом намеренно: respawn_panorbit пересоздаёт камеру при смене состояния, и правки на компоненте терялись бы; система дополнительно применяет параметры к только что появившейся камере (Mut::is_added).
  
  Конфиг остался источником начальных значений: добавлена секция [game.aircraft] (max_speed, roll_speed, pitch_speed, yaw_speed, вложенная [game.aircraft.thrust] с max_force, change_speed) — значения по умолчанию совпадают с прежними захардкоженными в Aircraft::new()/Thrust::new(), поэтому поведение не изменилось и Config.toml править не требуется.
  
  Разделение состояния и параметров: Aircraft стал маркером, Thrust теперь хранит только рантайм-состояние (current, target), настройки ушли в ThrustParams — редактор правит параметры, не конкурируя с игровой логикой.
  
  Регистрация типов также покрывает не-параметрические компоненты (Movement, Thrust, Follower, Followee, PreviousTransform, PanOrbitCamera, PanOrbitCameraTarget, SimpleCamera, FlickeringLight, маркеры Sun, Aircraft, Terrain) — они нужны редактору для поиска сущностей, и как побочный эффект теперь видны в WorldInspectorPlugin (Backquote), где раньше не отображались.

  Стоимость холостого кадра: три Changed<T>-запроса по 1–3 сущностям и одна проверка Res::is_changed — на профилировании неразличимо.
  
  Что стоит знать перед шагом 3                                                            
  
  - SunDisk в bevy 0.19 вообще не Reflect, а AtmosphereSettings не зарегистрирован как ReflectComponent — через BRP их нельзя было бы править напрямую; теперь они доступны через SunParams/CameraParams.
  - GlobalAmbientLight и DirectionalLightShadowMap — штатные Reflect-ресурсы bevy, отдельный слой им не нужен.
  - Правки параметров сущностей, живущих внутри состояния (самолёт, сопла, ландшафт), теряются при переключении Hangar/InGame — они пересоздаются из конфига. Это закрывается шагом 4 (обратная сериализация в
    TOML); скажите, если делать.

---

Зачем нужен компонент Sun, если есть SunParams?

---

● Сейчас — незачем, честный ответ: Sun не запрашивается нигде (grep находит только спавн и register_type). Это доставшийся от прежнего кода маркер (main.rs объявлял struct Sun и тоже никогда его не
  использовал), я его просто сохранил при переносе в environment.rs.

  Функционально SunParams уже является уникальным ключом: солнце — единственная сущность с этим компонентом, и редактор находит её через bevy/query по SunParams ничуть не хуже, а человекочитаемую метку даёт
  Name::new("Sun").

  Аргументы, при которых маркер перестаёт быть лишним:

  - Идентичность отделена от параметров. Если параметры снимут или заменят другим набором (например, MoonParams для ночного светила), сущность всё ещё «солнце». Пока такого сценария нет.
  - Несколько сущностей с одинаковыми параметрами. Если появится второй направленный свет с теми же полями, SunParams перестанет быть уникальным, а маркер — нет.

  Та же история с Terrain (был приватным и неиспользуемым до правок) и с Aircraft, который я превратил в маркер из компонента с данными — он тоже пока нигде не запрашивается, хотя для него сценарий
  использования близок: боты-противники и поражение целей из бэклога потребуют отличать самолёты от прочих сущностей.

  Предлагаю убрать Sun и Terrain вместе с их register_type, а Aircraft оставить как маркер класса сущностей. Убрать?

---

